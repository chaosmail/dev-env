---
# Install Java Runtime

- name: Install latest jdk/jre package
  apt: pkg="{{ item }}" state=present force=yes
  with_items:
    - openjdk-8-jdk 
    - openjrk-8-jre 
    - openjdk-9-jdk 
    - openjrk-9-jre 
  sudo: true