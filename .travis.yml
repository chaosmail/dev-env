dist: xenial

addons:
  apt:
    update: true

addons:
  ssh_known_hosts:
  - github.com

script:
  - git clone git@github.com:chaosmail/dev-env.git ~/.dev-env
  - sh ~/.dev-env/scripts/setup.sh
  - dev-env